% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qcScatter.R
\name{qcScatter}
\alias{qcScatter}
\title{QC Scatter Plot with Marginal Densities}
\usage{
qcScatter(
  metadata,
  Xvar,
  Yvar,
  groupVar = "orig.ident",
  logTransformX = FALSE,
  logTransformY = FALSE,
  colors = NULL,
  outDir = NULL,
  width,
  height
)
}
\arguments{
\item{metadata}{A data frame of per-cell or per-sample QC metrics.}

\item{Xvar}{Character string. Column name in \code{metadata} for the x-axis.}

\item{Yvar}{Character string. Column name in \code{metadata} for the y-axis.}

\item{groupVar}{Character string. Column name in \code{metadata} used for color
and faceting (default \code{"orig.ident"}).}

\item{logTransformX}{Logical. Apply \code{\link[ggplot2:scale_continuous]{ggplot2::scale_x_log10()}} (default
\code{FALSE}).}

\item{logTransformY}{Logical. Apply \code{\link[ggplot2:scale_continuous]{ggplot2::scale_y_log10()}} (default
\code{FALSE}).}

\item{colors}{Optional named or positional color vector for groups (default
\code{NULL}, in which case, 8 default colors are used.)}

\item{outDir}{Optional directory path for saving the plot. If \code{NULL}
(default), the plot is not saved.}

\item{width}{Numeric. Plot width in inches (default \code{12}).}

\item{height}{Numeric. Plot height in inches (default \code{10}).}
}
\value{
A combined \code{\link[cowplot:plot_grid]{cowplot::plot_grid()}} object (returned invisibly when
saved to disk).
}
\description{
Generates a two-panel QC scatter plot for any pair of metadata variables.
The top panel shows all samples overlaid with marginal density curves
(via \code{\link[ggExtra:ggMarginal]{ggExtra::ggMarginal()}}). The bottom panel facets by the grouping
variable. Axes can optionally be log10-scaled. This function assumes
you have extracted the metadata from your Seurat object and saved as a
dataframe.
}
\examples{
\dontrun{
qcScatter(metadata,
  Xvar = "log_atac_peak_region_fragments",
  Yvar = "pct_reads_in_peaks",
  groupVar = "orig.ident",
  colors = my_colors
)
}

}
