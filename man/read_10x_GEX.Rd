% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_10x_GEX.R
\name{read_10x_GEX}
\alias{read_10x_GEX}
\title{Read a 10x Genomics Gene Expression Sample into a Seurat Object}
\usage{
read_10x_GEX(sampleDir, sample, ident, min.cells, min.features)
}
\arguments{
\item{sampleDir}{Character string. Path to the parent directory that contains
one subdirectory per sample.}

\item{sample}{Character string. Name of the sample subdirectory within
\code{sampleDir}.}

\item{ident}{Character string. Identity label to assign to every cell in the
resulting Seurat object (stored in the \code{ident} metadata column).}

\item{min.cells}{Integer. Include features detected in at least this many
cells. Passed to \code{\link[Seurat:reexports]{Seurat::CreateSeuratObject()}}.}

\item{min.features}{Integer. Include cells where at least this many features
are detected. Passed to \code{\link[Seurat:reexports]{Seurat::CreateSeuratObject()}}.}
}
\value{
A \link[Seurat:reexports]{Seurat} object for the specified
sample with the \code{RNA} assay, an \code{ident} metadata column, and a
\code{log10GenesPerUMI} complexity metric.
}
\description{
Reads a single sample's filtered feature barcode matrix H5 file from a 10x
Genomics Cell Ranger output directory and creates a
\link[Seurat:reexports]{Seurat} object. A log10 genes-per-UMI
complexity metric is added to the object metadata as \code{log10GenesPerUMI}.
}
\details{
The expected input path is
\verb{<sampleDir>/<sample>/outs/filtered_feature_bc_matrix.h5}. To process
multiple samples, iterate over sample names with \code{\link[purrr:map]{purrr::map()}} or
\code{\link[=lapply]{lapply()}}.
}
\examples{
\dontrun{
library(purrr)

sample_dir <- "/path/to/data/"
samples    <- c("S1", "S2", "S3")
idents     <- c("CondA", "CondB", "CondC")

seurat_list <- map(samples, idents, ~ read_10x_GEX(
  sampleDir    = sample_dir,
  sample       = .x,
  ident        = .y,
  min.cells    = 10,
  min.features = 100
))
}

}
